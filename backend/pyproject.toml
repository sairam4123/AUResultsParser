[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[all]>=0.135.1",
    "pdfplumber>=0.11.9",
    "tabulate>=0.10.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
